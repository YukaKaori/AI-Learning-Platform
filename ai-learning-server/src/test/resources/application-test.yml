# Test profile: in-memory H2 in MySQL compatibility mode so the suite runs without a local MySQL.
spring:
  datasource:
    url: jdbc:h2:mem:ai_learning;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    enabled: false
