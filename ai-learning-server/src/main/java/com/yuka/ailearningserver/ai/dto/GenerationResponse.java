package com.yuka.ailearningserver.ai.dto;

/** Plain-text generation result — explain, summary, suggestions, note actions, analytics narratives. */
public record GenerationResponse(String content) {
}
