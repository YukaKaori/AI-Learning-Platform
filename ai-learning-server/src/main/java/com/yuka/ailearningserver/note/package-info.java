/**
 * Notes — markdown-first knowledge capture, optionally attached to a subject.
 *
 * <p>Phase 5 is schema + entity only; the editor experience lands with the
 * notes API. AI features over notes (summarize, link, generate flashcards)
 * are Phase 6+ and consume this domain — they do not live here.
 * Reserved error-code range: 130000–139999.
 */
package com.yuka.ailearningserver.note;
