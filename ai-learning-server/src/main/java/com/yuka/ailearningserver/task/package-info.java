/**
 * Learning tasks — lightweight to-dos with optional subject and due date.
 * Tasks surface in the workspace ("today"), and on the study calendar.
 *
 * <p>Phase 5 is schema + entity only. This is deliberately NOT a project
 * management system: no assignees, no nesting, no workflows.
 * Reserved error-code range: 150000–159999.
 */
package com.yuka.ailearningserver.task;
